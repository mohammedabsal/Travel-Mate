# Backend (minimal)

This directory contains a minimal Express backend used for development. It provides a `GET /health` endpoint and a sample `GET /api/trips` route.

To run:

```bash
cd backend
npm install
npm start
```

You can change the listening port with the `PORT` env var (default: `4000`).
